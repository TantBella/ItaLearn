import {When, Given, Then} from "@badeball/cypress-cucumber-preprocessor"


Given('Jag är på success-sidan', () => {
  cy.visit('http://localhost:5173/#/success');
});

When('', () => {

});

Then('', () => {

});
